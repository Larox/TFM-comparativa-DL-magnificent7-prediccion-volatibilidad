# Notebooks

Notebooks here are **exploration only**. They are NOT the source of truth.

All logic must live in `tfm_volatility/`. If a notebook produces something worth keeping, port it into the package and add a test.

Scratch notebooks ending in `-scratch.ipynb` are gitignored.
